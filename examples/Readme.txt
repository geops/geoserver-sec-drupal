Contains the same structure as /var/lib/tomcat7/webapps/geoserver/

There is one Authentication Provider per Drupal instance and one User Group Service per Drupal instance.
There is only one Role Service per GeoServer instance that muxes to the User Group Services.